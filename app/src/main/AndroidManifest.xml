<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bringin.iak.giziremaja">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="GIZI PADA REMAJA"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".faktor" />
        <activity android:name=".Kategori" />
        <activity android:name=".imt" />
        <activity android:name=".status" />
        <activity android:name=".angka_kecukupan" />
        <activity android:name=".penata_laksana" />
        <activity android:name=".dampak" />
        <activity android:name=".penutup" />
        <activity android:name=".indeks"></activity>
    </application>

</manifest>